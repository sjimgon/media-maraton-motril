---
const { 
  href = undefined, 
  type = "button", 
  class: additionalClass = '',
  id = ""
} = Astro.props;

const baseClass = "font-bold bg-white text-primarioMMM px-4 py-2 rounded-lg ring hover:bg-primarioMMM hover:text-white transition-colors";
---

{href ? (
  <a 
    href={href}
    id={id}       
    class={`${baseClass} ${additionalClass}`}
  >
    <slot />
  </a>
) : (
  <button 
    type={type}
    id={id} 
    class={`${baseClass} ${additionalClass}`}
  >
    <slot />
  </button>
)}
